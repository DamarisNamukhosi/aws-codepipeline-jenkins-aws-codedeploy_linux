pipeline {
  agent any
  stages {
    stage('git clone') {
      steps {
        sh 'cd /var/www/html'
        sh 'git clone https://github.com/DamarisNamukhosi/Codedeploy-Codepipeline.git /home/ubuntu/test'
       
      }
    }
  }
}
